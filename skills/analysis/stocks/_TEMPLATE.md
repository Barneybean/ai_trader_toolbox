# <TICKER — Company name> — stock playbook

_Copy to `stocks/<TICKER>.md`, fill in, register in `stocks/README.md`. This is **knowledge about
how a specific name trades and prices** — dated history and repeating structure, NOT a live call.
Live theses belong in `skills/playbook/watchlist-theses.md` or your private overlay._

**What sets the price:** the 1–2 name-specific variables (link the sector playbook in
`../sectors/` for the industry backdrop — don't repeat it).

**The repeating setups:** patterns this name cycles through (e.g. sells the news on earnings,
chip-wash before every product cycle, squeezes on datacenter headlines).

**Catalyst calendar (+ primary source that reveals each first):** the recurring dated events —
earnings cadence, product launches, regulatory dates — and where the data appears before headlines.

**Valuation ranges:** how the market has actually valued it across regimes (multiple bands,
sum-of-parts, where the floor held and the ceiling capped).

**Red flags / historical invalidation:** what has broken the story before, and the tell that
preceded it.

**Worked episodes (dated):** at least one case study — the setup, what happened, the lesson.

**Variant angle:** what the crowd persistently gets wrong about this name
(`skills/edge/variant-perception.md`).
