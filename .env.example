# Copy to `.env` (git-ignored) and fill in. Secrets go here, never in tracked files.

# LLM key — only needed for the OPTIONAL autonomous runner.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Broker credentials, if your adapter uses env instead of an interactive/MCP login.
# ROBINHOOD_TOKEN=
# IBKR_ACCOUNT=
# FUTU_ACCOUNT=

